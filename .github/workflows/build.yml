on:
  push:
    branches:
      - trunk
  # pull_request
  # workflow_dispatch

jobs:
  build:
    # NOTE: Match with config/west.yml
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3.0
